/**
 * Button 계열의 시맨틱 계약은 `@berrypjh/ui-core`가 가집니다.
 * 이 파일은 기존 import 경로와 이름을 유지하기 위한 재수출입니다.
 */
export type {
  ButtonColor,
  ButtonLoadingPosition,
  ButtonSemanticProps as ButtonProps,
  ButtonSize,
  ButtonVariant,
} from '@berrypjh/ui-core';
