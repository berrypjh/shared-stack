import { fabPresentation } from '../presentation/components/fab';
import { DeveloperComponentPage } from '../presentation/DeveloperComponentPage';

/**
 * example 목록은 이 파일에 없다 — `presentation/components/fab` 가 유일한 source 다. */
export const FabPage = () => <DeveloperComponentPage presentation={fabPresentation} />;
