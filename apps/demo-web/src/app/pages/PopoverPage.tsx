import { popoverPresentation } from '../presentation/components/popover';
import { DeveloperComponentPage } from '../presentation/DeveloperComponentPage';

/**
 * example 목록은 이 파일에 없다 — `presentation/components/popover` 가 유일한 source 다. */
export const PopoverPage = () => <DeveloperComponentPage presentation={popoverPresentation} />;

export default PopoverPage;
