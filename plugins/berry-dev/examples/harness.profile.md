# Harness profile

<!-- berry-dev examples/harness.profile.md를 복사한 것. `<...>`를 이 저장소의 사실로 바꾸고, 쓰지 않는 절은 지운다. -->

berry-dev의 generic rule · 절차가 가리키는 저장소 사실이다. 명령을 실행하는 형식이 아니라 사람이 읽는 문서다. 사실이 바뀌면 이 파일만 고친다.

## 검증

`repo-verify`가 이 절을 읽는다. target 표를 옮겨 적지 말고 확인하는 명령과 근거 파일을 적는다.

### 영향 범위

- 영향 확인 — `<예: nx show projects --affected --files=<파일>, 또는 workspace scripts>`
- target 확인 — `<예: nx show project <이름> --json>`
- affected가 알려 주지 않는 것 — `<root 프로젝트로 잡히는 경로, 저장소 전체를 입력으로 두는 프로젝트 등>`

### 프로젝트 밖 경로

| 바꾼 곳                              | 검사                    |
| ------------------------------------ | ----------------------- |
| `<도구 · 설정 · hook · plugin 경로>` | `<그 경로를 보는 명령>` |

### 의존 순서

- `<검사 전에 먼저 만들어야 하는 산출물과 명령>`

### build 종류

- `<로컬 산출물인 build target>`
- `<클라우드 빌드 · 배포 · 게시 명령 — 검증으로 실행하지 않음>`

### AI 세션에서 실행할 수 없는 것

- `<dev 서버 · e2e 처럼 포트를 여는 명령>` — 사용자에게 실행을 요청하고, 결과를 받기 전에는 unsupported로 보고
- `<실행기 · 기기 · 계정이 없어 돌릴 수 없는 검사>` — unsupported로 보고

## UI

`frontend-quality`가 이 절을 읽는다. 컴포넌트 · prop · 토큰 목록은 적지 않는다.

### 역할

| 경로                      | 역할       | 따를 문서            |
| ------------------------- | ---------- | -------------------- |
| `<UI 패키지 source 경로>` | maintainer | `<패키지 AGENTS.md>` |
| `<패키지를 쓰는 앱 경로>` | consumer   | `<앱 AGENTS.md>`     |

### consumer 조회

- `<설치된 bin 을 앱 패키지 안에서 부르는 명령>`

### locale 과 제품 정책

- locale — `<예: ko-KR, 또는 정하지 않음>`
- 셸 · 화면 폭 · 최소 터치 크기 · 완료 문구 — `<값과 근거 문서, 또는 없음>`

### 확인 수단

| 방식 | 있는 것                         | 이 세션에서                 |
| ---- | ------------------------------- | --------------------------- |
| 자동 | `<컴포넌트 테스트 · 규칙 검사>` | `<실행 가능 · unsupported>` |
| 실제 | `<e2e · 기기 · 스크린 리더>`    | `<실행 가능 · unsupported>` |

## 한국어 화면 (ko-ui 를 쓸 때)

- 어미 — `<예: ~합니다 또는 ~해요 중 하나>`
- 날짜 · 금액 · 전화번호 형식 — `<이 저장소가 쓰는 형식>`
- 최소 터치 타깃 · line-height · 좁은 화면 기준 — `<값>`
- 어절 보호 방식 — `<CSS 나 플랫폼 설정>`

## 한국어 문서 (docs-ko 를 쓸 때)

- 적용하지 않는 문서 — `<예: 영어 README, 고쳐 쓰지 않는 기록>`
